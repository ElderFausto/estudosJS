// (condicao) ? 'valor verdadeiro' : 'valor falso';

const pontuacao = 300;
const nivelPontuacao = pontuacao >= 1000 ? 'Usuario VIP' : 'Usuario normal';
console.log(nivelPontuacao)

/* if (pontuacao >= 1000) {
    console.log('usuario vip!');
} else {
    console.log('usuario normal');
} */